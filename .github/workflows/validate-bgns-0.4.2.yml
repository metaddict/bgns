name: bgns validation runner smoke test

on:
  push:
    branches:
      - validation/bgns-0.4.2-runtime
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner startup
        run: echo "runner started"
