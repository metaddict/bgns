name: Validate bgns 0.4.2 runner

on:
  push:
    branches:
      - agent/bgns-042-runtime-ci-20260804

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner startup
        run: echo "runner started"
